const nome = "Mônica";
const sobrenome = "Lafrande";
const idade = 33;
let peso = 58;
const altura = 1.63;
let imc = peso / (altura * altura);

console.log(
  `${nome} ${sobrenome} tem ${idade}anos, pesa ${peso}kg, tem ${altura} de altura e seu IMC é de ${imc}.`
);
